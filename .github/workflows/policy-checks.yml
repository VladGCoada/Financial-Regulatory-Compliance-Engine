name: Policy Checks

on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: OPA placeholder
        run: grep -R "plaintext PII" policies/opa || true
